import './Breadcrumb.css'
const Breadcrumb = (props) => {

    return(
        <>
        <div class="header">
            <h1>{props.title}</h1>
        </div>
        </>
    );
}

export default Breadcrumb;